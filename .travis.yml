language: python
python:
  - "2.5"
  - "2.6"
  - "2.7"
env:
  - EVENTLET_HUB=selects
  - EVENTLET_HUB=poll
  - EVENTLET_HUB=epolls
install: pip install . --use-mirrors
script:  python setup.py nosetests
